export type CircleConfig = {
  radius: number;
  stroke: { width: number; color: string };
  fill: string;
};

export const CIRCLE_CONFIG = {
  radius: 8,
  stroke: {
    width: 1,
    color: "#000000",
  },
  fill: "#FFFFFF",
};
